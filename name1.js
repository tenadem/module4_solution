var myNames = ["Mark","Marco","Frank","John","Sandra","Jack"];
for (var i = 0; i < myNames.length ; i++) {
	if(myNames[i].startsWith("J")){
		consol.log("Goodbye" + myNames[i]);

	}else{
		consol.log("Hello" + myNames[i] )
	}
}